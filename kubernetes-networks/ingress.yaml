apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules:
  - http:
      paths:
      - path: /testpath
        backend:
          serviceName: my-service
          servicePort: 80
status:
  loadBalancer:
    ingress:
    - hostname: placeholder
